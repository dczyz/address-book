<Window x:Class="AddressBook.ApplicationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:login="clr-namespace:AddressBook.Login"
        xmlns:signUp="clr-namespace:AddressBook.SignUp"
        xmlns:mainView="clr-namespace:AddressBook.MainView"
        Title="Address Book" SizeToContent="WidthAndHeight">

    <Window.Resources>
        <DataTemplate DataType="{x:Type login:LoginViewModel}">
            <login:LoginView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type signUp:SignUpViewModel}">
            <signUp:SignUpView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type mainView:MainViewModel}">
            <mainView:MainView />
        </DataTemplate>
    </Window.Resources>
    <ContentControl Content="{Binding CurrentPageViewModel}" />
</Window>
