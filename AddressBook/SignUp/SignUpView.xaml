<UserControl x:Class="AddressBook.SignUp.SignUpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:addressBook="clr-namespace:AddressBook"
             mc:Ignorable="d" 
              Height="330" Width="400">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Grid Background="White">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10,0,0">
            <Label x:Name="UsernameLabel" Content="Username" HorizontalAlignment="Center"/>
            <TextBox MaxLength="255" x:Name="UsernameTextBox" Height="23" TextWrapping="Wrap" Text="{Binding Username}" Margin="0,0,0,10" Width="250">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding SignUpCommand}" />
                </TextBox.InputBindings>
            </TextBox>
            <Label x:Name="PasswordLabel" Content="Password" HorizontalAlignment="Center"/>
            <PasswordBox MaxLength="255" x:Name="PasswordPasswordBox" Height="23" addressBook:PasswordHelper.Attach="True" 
                   addressBook:PasswordHelper.Password="{Binding Text, ElementName=plainPassword, Mode=TwoWay}" Margin="0,0,0,10" Width="250">
                <PasswordBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding SignUpCommand}" />
                </PasswordBox.InputBindings>
            </PasswordBox>
            <TextBlock Name="plainPassword" Height="0" Foreground="Transparent" Visibility="Hidden" Text="{Binding Password, Mode=TwoWay}" />
            <Label x:Name="ConfirmPasswordLabel" Content="Confirm password" HorizontalAlignment="Center"/>
            <PasswordBox MaxLength="255" x:Name="ConfirmPasswordPasswordBox" Height="23" addressBook:PasswordHelper.Attach="True" 
                   addressBook:PasswordHelper.Password="{Binding Text, ElementName=plainConfirmPassword, Mode=TwoWay}" Margin="0,0,0,10" Width="250">
                <PasswordBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding SignUpCommand}" />
                </PasswordBox.InputBindings>
            </PasswordBox>
            <TextBlock Name="plainConfirmPassword" Height="0" Foreground="Transparent" Visibility="Hidden" Text="{Binding ConfirmPassword, Mode=TwoWay}" />
            <Button x:Name="SignUpButton" Content="Sign Up" Command="{Binding SignUpCommand}" Width="150" Height="30" Margin="0,10"/>
            <Button x:Name="BackToLoginButton" Content="Back to Login" Command="{Binding BackToLoginCommand}" Width="150" Height="30" Margin="0,10,0,0"/>
            <Label x:Name="ErrorLabel" Content="{Binding ErrorText}" Foreground="Red" Visibility="{Binding Error, Converter={StaticResource BooleanToVisibilityConverter}}" HorizontalAlignment="Center" Margin="0,10,0,0"/>
        </StackPanel>

    </Grid>
</UserControl>
